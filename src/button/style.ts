import { BUTTON_BLUE } from '../colors';

const BoardButtonStyle = {
  backgroundColor: BUTTON_BLUE,
  boxShadow: 0,
  '&:hover': {
    backgroundColor: BUTTON_BLUE,
  },
};

export default BoardButtonStyle;
